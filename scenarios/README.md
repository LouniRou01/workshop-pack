# Scenarios

The scenario registry defines the workshop exercises and the expected decision for each one.

Start with the three required scenarios. Use the extension scenarios to demonstrate ownership conflicts, suppression rules, and safe stopping behavior.
